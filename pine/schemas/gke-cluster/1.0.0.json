{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["kind", "schema_version"],
    "properties": {
        "kind": {
            "type": "string",
            "enum": ["gke-cluster"]
        },
        "schema_version": {
            "type": "string",
            "enum": ["1.0.0"]
        }
    }
} 